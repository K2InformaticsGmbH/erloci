{application, erloci,
    [
        {description, "Erlang port driver for OCI"},
        {vsn, "0.1.3"},
        {applications, [
            kernel,
            stdlib
        ]},
        {env, [{logging, debug}]},
        {mod, {erloci, []}}
    ]}.
