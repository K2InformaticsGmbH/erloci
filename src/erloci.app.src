{application, erloci,
    [
        {description, "Erlang port driver for OCI"},
        {vsn, "0.1.1"},
        {applications, [
            kernel,
            stdlib
        ]},
        {env, [{logging, debug}]},
        {mod, {erloci, []}}
    ]}.
