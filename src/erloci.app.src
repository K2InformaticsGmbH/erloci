{application, erloci,
    [
        {applications, [
            kernel,
            stdlib
        ]},
        {description, "Erlang port driver for OCI"},
        {env, [{logging, debug}]},
        {mod, {erloci, []}},
        {vsn, "0.1.0"}
    ]}.
